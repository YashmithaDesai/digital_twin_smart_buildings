# render.yaml
# Configuration for Render deployment
# Reference: https://render.com/docs/deploy-python-flask

services:
  - type: web
    name: digital-twin-backend
    runtime: python
    pythonVersion: 3.11
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn digital_twin_smart_buildings.backend.main:app --host 0.0.0.0 --port 8000"
    envVars:
      - key: ENVIRONMENT
        value: production
      - key: API_HOST
        value: "0.0.0.0"
      - key: API_PORT
        value: "8000"
    databases:
      - name: postgres
        # Configure database connection in environment variables
